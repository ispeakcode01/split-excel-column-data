
//import readXlsxFile from 'read-excel-file'

export default {
    methods: {
      readFile() {
          alert('hello world')
        /*readXlsxFile(fileToRead).then(rows => {
            return rows;
        })*/
      }
    }
  };